# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

group :development do
  gem "capistrano", "~> 3.16.0"
  gem "capistrano-nest", github: "insomenia/capistrano-nest"
  gem "json"
end
